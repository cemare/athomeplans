import PlanBuilder from "@/components/PlanBuilder";

export const metadata = {
  title: "Custom Plan Builder | Athena Interior",
  description: "Create and export custom room layouts with interactive furniture placement and style controls.",
};

export default function BuilderPage() {
  return <PlanBuilder />;
}
